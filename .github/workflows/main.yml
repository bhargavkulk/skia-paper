name: Build LaTeX PDF (manual)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build PDF with pdflatex
        uses: xu-cheng/latex-action@v3
        with:
          root_file: main.tex
