#+title: Skia Paper

* TODOS [14/19]
- [X] is based on the most recent version of the formatting template/style (see details below),
- [X] does not change any style or formatting settings of the template,
- [ ] does not exceed the page limit specified in the acceptance-notification e-mail,
- [X] is in PDF format,
- [X] has all fonts embedded in the PDF file,
- [X] uses only scalable font types (like Type 1, TrueType) --- bit-mapped font types (like Type 3) are not acceptable,
- [X] provides the affiliation (institution and country) in the author information on the first page,
- [X] defines all authors individually (not as a list),
- [X] has the title and all headings properly capitalized,
- [X] does not use footnotes and references in the abstract,
- [X] provides a DOI for each item in the References section (if possible),
- [X] has all diagrams embedded in vector graphics
- [X] (if not possible, use a high-resolution bitmap format of at least 300 dpi; do not use JPG, but a lossless format like PNG or GIF; JPG is ok for photos),
- [X] has all text in figures and tables large enough and readable when printed,
- [X] has a caption for every figure or table,
- [ ] has all units consistently separated from their values with a half space (e.g., '\SI{33}{GB}'),
- [ ] has no orphans and widows,
- [ ] has reduced the use of bold face and bright colors in the text as much as possible, underlining is not used at all in the text,
- [ ] has all third-party material appropriately attributed (mention author, copyright holder, and origin).
* Skia Examples

#+begin_src c++
void draw(SkCanvas* canvas) {
    canvas->clear(SK_ColorWHITE);

    SkPaint redPaint;
    // redPaint.setColor(SK_ColorRED);

    SkRect redRect = SkRect::MakeXYWH(0, 0, 180, 180);
    canvas->drawRect(redRect, redPaint);
}
#+end_src


#+begin_src c++
  void draw(SkCanvas* canvas) {
      canvas->clear(SK_ColorWHITE);

      SkPaint redPaint;
      redPaint.setColor(SK_ColorRED);

      SkRect redRect = SkRect::MakeXYWH(0, 0, 180, 180);
      canvas->drawRect(redRect, redPaint);
  }
#+end_src

#+begin_src c++

#+end_src

* Notes
- Hoare-style braces around an image (LaTeX snippet):
#+begin_src latex
$\left\{\vcenter{\hbox{\frame{\includegraphics[scale=0.28]{drawing0.pdf}}}}\right\}$
#+end_src

- Hoare triple helper macros with list-based image states:
#+begin_src latex
\ExplSyntaxOn

% Render one state from a comma-list of image filenames.
% Example: \hoarestatefromlist{drawing0.pdf,drawing2.pdf}
\NewDocumentCommand{\hoarestatefromlist}{m}
 {
  \begin{tabular}{c}
    \clist_set:Nn \l_tmpa_clist { #1 }
    \int_zero:N \l_tmpa_int
    \clist_map_inline:Nn \l_tmpa_clist
     {
      \int_incr:N \l_tmpa_int
      \frame{\includegraphics[scale=0.28]{##1}}
      \int_compare:nNnT { \l_tmpa_int } < { \clist_count:N \l_tmpa_clist }
       { \\[0.6em] }
     }
  \end{tabular}
 }

\NewDocumentCommand{\hoarecode}{m}
 {
  \begin{tabular}{T}
  #1
  \end{tabular}
 }

\NewDocumentCommand{\hoaretriplelist}{m m m}
 {
  \begin{figure}[H]
  \centering
  \hoarestatefromlist{#1}\quad
  \hoarecode{#2}\quad
  \hoarestatefromlist{#3}
  \end{figure}
 }

\ExplSyntaxOff
#+end_src

#+begin_src c++
  void draw(SkCanvas* canvas) {
      SkPaint paint;

      SkPoint center = {128.0f, 128.0f};
      SkScalar radius = 128.0f;

      SkColor4f colors[] = {
          SkColors::kBlack,                  // opaque black
          SkColors::kBlack.withAlpha(0.0f)   // transparent black
      };

      SkScalar positions[] = {0.0f, 1.0f};

      auto shader = SkShaders::RadialGradient(
                                              center,
                                              radius,
                                              { {colors, positions, SkTileMode::kClamp}, {} },
                                              nullptr
                                              );

      paint.setShader(shader);

    	SkPaint savePaint;
    	savePaint.setBlendMode(
                             SkBlendMode::kDstIn
                             );
      SkSamplingOptions sampling;
      canvas->drawImageRect(image, SkRect::MakeXYWH(0, 0, 256, 256), sampling);

    	canvas->saveLayer(nullptr, &savePaint);
    	canvas->drawRect(SkRect::MakeXYWH(0, 0, 256, 256), paint);
    	canvas->restore();
  }
#+end_src

#+begin_src c++
void draw(SkCanvas* canvas) {
	SkPaint paint;
  	SkPaint savePaint;
  	savePaint.setBlendMode(
		SkBlendMode::kDstIn
	);
    SkSamplingOptions sampling;
    canvas->drawImageRect(image, SkRect::MakeXYWH(0, 0, 256, 256), sampling);

  	canvas->saveLayer(nullptr, &savePaint);
  	canvas->drawRect(SkRect::MakeXYWH(0, 0, 180, 180), paint);
  	canvas->drawRect(SkRect::MakeXYWH(120, 120, 136, 136), paint);
  	canvas->restore();
}
#+end_src

#+begin_src LaTeX
\begin{figure}
  \centering
  \begin{minipage}{0.40\linewidth}
    \begin{tabular}{S}
      Layer & ::= & Empty() \\
            & |   & Draw(l: \textsf{Layer}, s: \Shape,\\
            &     & \hspace{4em}p: \textsf{Paint}, c: \Shape)\\
            & |   & BlendLayer(l\textsubscript{bot}, l\textsubscript{top}: \textsf{Layer})
            &     & \hspace{8em}p: \textsf{Paint}) \\[2pt]
      Paint & ::= & Paint(fill: Image, filter: Filter, blend: Blend)
    \end{tabular}
    \label{fig:layer}
  \end{minipage}
  \hfill
  \begin{minipage}{0.58\linewidth}
    \centering
    \begin{tabular}{S M c T}
      Command & c & ::= & Draw($g$: Shape, $p$: Paint) \\
              &   & | & DrawText(string, $p$: Paint) \\
              &   & | & Clip($cl$: Shape) \\
              &   & | & Save(); $c$; Restore() \\
              &   & | & SaveLayer($p$: Paint); $c$; Restore() \\
              &   & | & $c$; $c$ \\[1pt]
    \end{tabular}
    \caption{%
      \textsf{Layer} provides
      a simple functional description of Skia images.
    }
    \label{fig:muskia}
  \end{minipage}
\end{figure}
#+end_src

* Abstract/Intro stuff

- line 31

* Acks

Thalia Archibald
