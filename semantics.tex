\documentclass[main.tex]{subfiles}
\begin{document}
\section{Semantics}
\label{sec:semantics}

\begin{figure}
  \centering
  \begin{minipage}{0.40\linewidth}
    \todo[inline]{I think we require a special \textsf{Transparent} color.}
    \centering
    \begin{tabular}{S S}
      Image  & : \; Point $\to$ Color \\[2pt]
      Shape  & : \; Point $\to$ Bool \\[2pt]
      Blend  & : \; Color $\times$ Color $\to$ Color \\[2pt]
      Filter & : \; Color $\to$ Color
    \end{tabular}
    \caption{%
      Our abstract model of \textsf{Image}s
      and \textsf{Shapes} is built from
      abstract types \textsf{Pixels} and \textsf{Colors}.
    }
    \label{fig:model}
  \end{minipage}
  \hfill
  \begin{minipage}{0.58\linewidth}
    \todo[inline]{I don't like that this image has to line-break.
      I suspect that a long-term fix is to make fig:model
      horizontally adjacent to a new figure showing
      the specific instantiation of the abstract model
      used in our actual Lean formalization.}
    \todo[inline]{Annoying and bad that we define two \textsf{Paint}
      objects, one here and one in \muskia.
      Add color filters to this semantics, drop styles from the other,
      and then they match and we only define it here.}
    \centering
    \begin{tabular}{S c T}
      Layer & ::= & Empty() \\
            & |   & Draw(l: \textsf{Layer}, s: \textsf{Shape},\\
            &     & \hspace{4em}p: \textsf{Paint}, c: \textsf{Shape})\\
            & |   & BlendLayer(l\textsubscript{bot}: \textsf{Layer},
                    l\textsubscript{top}: \textsf{Layer},\\
            &     & \hspace{8em}p: \textsf{Paint}) \\[2pt]
      Paint & ::= & Paint(fill: Image, blend: Blend)
    \end{tabular}
    \caption{%
      \textsf{Layer} provides
      a simple functional description of Skia images.
    }
    \label{fig:layer}
  \end{minipage}
\end{figure}

This paper's semantics of Skia
  consists of three components~\Cref{fig:diagram}.
The bottom-most layer is an abstract model of
  images, shapes, blends, and filters defined in terms
  of abstract types \textsf{Point} and \textsf{Color},
  which can be instantiated to model
  as much of the actual Skia API as necessary.
The middle layer is functional operations
  \texttt{Draw} and \texttt{BlendLayer}
  and an abstract type \textsf{Paint},
  which denote to our abstract model.
The top layer is an imperative language, \muskia,
  whose operational semantics provides a model of Skia.
This section describes all three layers
  and then examines ???\todo{Count them} rewrite rules
  proven valid using the semantics.

\subsection{Abstract Model}

Traditional programming language semantics
  typically do not have to define their abstract model,
  since it is typically based on commonly-known types
  such as pairs, functions, and integers.
In the graphics domain, the core types
  are not so well established,
  so the abstract model must be explicitly defined.
The abstract model of our semantics
  is shown in \Cref{fig:model};
  it is based around two core abstract types,
  \textsf{Point} and \textsf{Color}.
The \textsf{Point} and \textsf{Color} types
  are then used to define the key types in Skia.
An \textsf{Image} is defined
  as a \textsf{Color} for every \textsf{Point}
  while \textsf{Shape}s are defined
  as predicates on \textsf{Point}s.
Equivalence of two \textsf{Image}s is defined
  as assigning the same \textsf{Color} to each \textsf{Point}.
Additionally, since a major focus of this semantics
  is reasoning about color blending and filtration,
  types for \textsf{Blend}s and \textsf{Filter}s are defined
  as binary and unary operators on colors.

This abstract model hides substantial complexity
  in the actual implementation.%
\footnote{
In the same way that a traditional programming language semantics
  might hide the complexity of actually storing closures on the heap.
}
The actual Skia implementation
  defines \textsf{Point} to be a pair of 32-bit signed integers
  and \textsf{Color} to be a 32-bit unsigned integer
  interpreted as ARGB pre-multiplied byte-sized color,%
\footnote{
``ARGB'' gives the in-memory byte order
  of the alpha, red, green, and blue components of a color.
The interpretation of these bytes as an unsigned 32-bit integer
  will depend on the endianness of the platform itself.
The colors are premultiplied, meaning
  that a color with an alpha value of $\alpha$
  will have its red, green, and blue components
  range from $0$ to $\alpha$;
  this form makes for somewhat more efficient computation.
}
  and operations on points and colors
  must thus be anti-aliased and rounded.
The exact anti-aliasing and rounding behavior
  is not guaranteed by Skia
  and is also exceptionally complex,
  including hardware dependence in some back-ends;
  the abstract model allows our semantics
  to ignore these details.
The equivalences proved are thus
  \emph{up to} anti-aliasing and rounding details
  (and also assume away issues like coordinate overflow).

Matching the abstract model to Skia
  would require actually defining
  the vast array of \textsf{Image}s
  (solid color fills, gradients, image files),
  \textsf{Shape}s (rectangles, paths, text outlines),
  \textsf{Blend}s (roughly two dozen),
  and \textsf{Filter}s (in fact, these can be defined
  by shader programs in a custom SkSL shader language).
Defining these objects and their equivalences
  would be a complex undertaking,
  especially given the pace of change in Skia itself.
Luckily, the compiler described in \Cref{sec:compiler}
  does rather superficial reasoning over these objects
  and only special-cases a handful of them.
Our Lean formalization only defines those special cases.
The abstract model nonetheless allows for extensibility;
  for example, this section does not discuss opacity,
  but our Lean formalization does include it.
This extension does not require any changes to the abstract model,
  only the addition of specific color and blend operators,
  and thus re-uses the denotational and operations semantics
  defined in the remainder of this section.

\subsection{Skia as Layers}

\begin{figure}
  \begin{align*}
    \denote{\texttt{Empty()}}(\textsf{pt})
    &= \mathsf{Transparent}\\[2pt]
    \denote{\texttt{BlendLayer(l\textsubscript{bot}, l\textsubscript{top},
    p)}}(\mathsf{pt})
    &= \mathtt{blend_p}\left(\denote{\mathtt{l_{bot}}}(\mathsf{pt}),\;
        \denote{\mathtt{l_{top}}}(\mathsf{pt})
      \right) \\[2pt]
    \denote{\texttt{Draw(l, s, p, c)}}(\mathsf{pt})
    &= \mathtt{blend_p}\left(\denote{\mathtt{l}(\mathsf{pt})},\;
       \begin{cases}
         \mathtt{fill_p}(\mathsf{pt}) & \mathsf{pt} \in \mathtt{s} \cap \mathtt{c} \\
         \mathsf{Transparent} & \textsf{otherwise}
       \end{cases}
       \right)\\
  \end{align*}
  \caption{}
  \label{fig:denote}
\end{figure}

Skia has two core operations:
  drawing a shape onto an image,
  and blending two images (representing two layers) into one.
Each operation is parameterized by a \textsf{Paint} object,
  which indicates the current blend mode,
  the fill type, and so on;
  drawing also requires the clip region from the canvas state.
The signatures for each operation are defined
  in \Cref{fig:layer} as constructors of
  a \textsf{Layer} type.
Equivalence of two \textsf{Layer} terms
  is defined by equivalence of their denotations to \textsf{Image}s.

The operations can be denoted to the abstract model,
  as shown in \Cref{fig:denote}.
\textsf{Empty} has a straightforward denotation
  to an all-\textsf{Transparent} image.
\textsf{BlendLayer} is likewise straightforward,
  applying a blend operator, determined by the paint $p$,
  point-wise on the two layers being blended.
\textsf{Draw} is most complex.
Conceptually, it first performs clipping,
  constructing the intersection $s \cap c$
  of the drawn shape and the clip state;
  then it performs filling,
  computing the fill color for each point in the shape;
  and finally it performs blending
  on the original color and computed fill for each point.
Note that the blend is performed
  even for points outside $s \cap c$;
  this is critical for, for example, \textsf{DstIn} blending.

\subsection{\muskia}

We now define \muskia which is our representation of the Skia API.

\begin{figure}
  \begin{tabular}{S M c T}
    Command & c & ::= & Draw($g$, $p$) \\
            &   & | & DrawText(string, $p$) \\
            &   & | & Clip($cl$, Intersect | Difference) \\
            &   & | & Save(); $c$; Restore() \\
            &   & | & SaveLayer($p$); $c$; Restore() \\
            &   & | & $c$; $c$ \\[1pt]
    Geometry & g,\;cl & ::= & Rect($\ldots$) | RRect($\ldots$) | Path($\ldots$) \\[2pt]
    Paint & p & ::= & Paint($f$, $st$, $c_f$, $b$) \\[2pt]
    Fill & f & ::= & Color(Pixel) | LinearGradient($\ldots$) | $\ldots$ \\[2pt]
    Filter & f & ::= & Id | Luma | $\ldots$ \\[2pt]
    BlendMode & b & ::= & SrcOver | DstIn | Src | $\ldots$\\
  \end{tabular}
  \label{fig:muskia}
\end{figure}

\noindent \muskia simplifies certain aspects of the actual Skia API:
The Skia API has separate \texttt{Draw} and \texttt{Clip} commands for each
  \textsf{Geometry},
  and not the overloaded function we present in the grammar.
The Skia API does not have structured constructs for \texttt{Save} and \texttt{SaveLayer}.

More crucially, we have omitted certain effects and commands from \muskia that
  are in Skia.
The \texttt{Style} tells Skia whether to fill or stroke the shape.
It can be considered as a function over \texttt{Geometry},
  basically modifying it.
Similarly,
  \texttt{MaskFilters} allow us to implement Gaussian blurs.
This could also be understood as functions over \texttt{Geometry},
  and omitting them does not affect our semantics at all.

Skia also provides facilities to transform, rotate, and perform other affine
  transformations.
It is well known that these transformations can be done using
  matrix multiplications.
So Skia maintains a transform state along with the layer and clip state.
The \texttt{Concat44} command multiplies a 4x4 matrix with the transform state,
  and affect the drawing of all commands after.
The transform state as a whole can be removed by multiplying
  the transform state of a command into the geometry
  and the clip, which is what we do.

Finally,
  we also omit image filters from our discussion of semantics.
Image filters are the final way to transform a layer prior to blending.
When a paint has an image filter,
  all drawing happens in a seperate pixel buffer,
  and the effect defined by the filter is applied to the entire buffer
  before its blended.
Image filters seem similiar to color and mask filters,
  but they allow more complicated effects,
  as they can act on groups of pixels instead of single pixels.

\subsection{Operational Semantics for Skia}

We now define an operational semantics for the Skia API.
These semantics will allows us to convert terms in \muskia
  to terms in \textsf{Layer}.
We first define a step relation $\longmapsto$:

\begin{align*}
\longmapsto&:\textsf{Command} \times \Sigma \to \Sigma
\end{align*}

\noindent$\Sigma$ is the canvas state:

\begin{align*}
\Sigma&:\textsf{List[Layer]} \times \textsf{List[Geometry]}
\end{align*}

\noindent
The canvas state has a reference to another canvas state,
  emulating a stack of layers and clips.
The following rules, define $\longmapsto$ over each command on \textsf{Command}.

\begin{mathpar}
  \inferrule
  { }
  {
    \langle
    \texttt{Draw(g, p)},
    \langle l :: L, c :: C \rangle
    \rangle
    \;\longmapsto\;
    \langle \texttt{Draw(l, g, p, c)} :: L, c :: C \rangle
  } \and
  \inferrule
  { }
  {
    \langle
    \texttt{Clip(c', Intersect)},
    \langle L, c :: C\rangle
    \rangle
    \;\longmapsto\;
    \langle L, c \cap c' :: C \rangle
  } \and
  \inferrule
  { }
  {
    \langle
    \texttt{Clip(c', Difference)},
    \langle L, c :: C \rangle
    \rangle
    \;\longmapsto\;
    \langle L, c / c' :: C \rangle
  } \and
  \inferrule
  {
    \langle
    \texttt{cmd},
    \langle L, c :: c :: C \rangle
    \rangle
    \;\longmapsto\;
    \langle L', c' :: c :: C \rangle
  }
  {
    \langle
    \texttt{Save(); cmd; Restore()},
    \langle L, c :: C\rangle
    \rangle
    \;\longmapsto\;
    \langle L', c :: C \rangle
  } \and
  \inferrule
  {
    \langle
    \texttt{cmd},
    \langle \texttt{Empty()} :: l :: L, c :: c :: C \rangle \rangle
    \;\longmapsto\;
    \langle l' :: l :: L, c' :: c :: C \rangle
  }
  {
    \langle
    \texttt{SaveLayer(p); cmd; Restore()},
    \langle l :: L, c :: C\rangle
    \rangle
    \;\longmapsto\;
    \langle \texttt{BlendLayer(l, l', p)} :: L, c :: C \rangle
  } \and
  \inferrule
  {
    \langle
    \texttt{cmd},
    \langle l :: L, c :: C \rangle \rangle
    \;\longmapsto\;
    \langle l' :: L, c' :: C \rangle \\
    \langle
    \texttt{cmd'},
    \langle l' :: L, c' :: C \rangle \rangle
    \;\longmapsto\;
    \langle l'' :: L, c'' :: C \rangle
  }
  {
    \langle
    \texttt{cmd; cmd'},
    \langle l :: L, c :: C \rangle
    \rangle
    \;\longmapsto\;
    \langle l^{\prime} :: L, c'' :: C \rangle
  }
\end{mathpar}

\noindent
$\longmapsto^*$ is the reflexive transitive closure of $\longmapsto$.
It can be proven that given the initial state $\langle [\texttt{Empty()}],
  [\mathcal{U}] \rangle$, $\longmapsto^*$ will never get stuck i.e.


\begin{equation*}
\forall\,\texttt{cmd}\,\exists\,l,\, c\;\text{s.t.}\;\langle \texttt{cmd}, \langle
[\texttt{Empty()}], [\mathcal{U}] \rangle \rangle \longmapsto^* \langle [l], [c] \rangle
\end{equation*}

\noindent
Given this fact,
  we can write down an evaluation function for terms in \muskia to
  \textsf{Image}.

\begin{gather*}
\textsf{eval} : \textsf{Command} \to \textsf{Image} \\
\textsf{eval}(c) = \llbracket l \rrbracket \;\textsf{where}\;\langle c, \langle [\texttt{Empty()}], [\mathcal{U}]
\rangle \rangle \longmapsto^* \langle [l], [C] \rangle
\end{gather*}

\noindent
We now have all the pieces to define \eqimg.

\begin{gather*}
\eqimg : \textsf{Command} \to \textsf{Command} \to \textsf{Command} \\
l \eqimg l' := \textit{eval}(l) = \textit{eval}(l')
\end{gather*}

\subsection{Rewrites in \muskia}

In this section we will discuss we will write down and prove some rewrites over
  Skia programs.
The goal of these rewrites is reduce the number of new layers created by
  \texttt{saveLayer()}.
We came up with these rewrites by carefully reading the Skia program dumps from
  the top 100 websites visited in the world.

\subsubsection{Opaque \texttt{saveLayer}}



\end{document}

# head section
just describe the abstract model, talk about how its abstracted over a Points
and Color

## Layer
abstract model describes a bunch of invalid skia programs, we need some way to
constrain it.

describe the denotational semantics here

## \mu Skia
syntax,
operational semantics,
limitations

## proving equivalences
figure of rewrite rules, and talk through them.
